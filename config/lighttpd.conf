server.modules = (
    "mod_access",
    "mod_alias",
    "mod_fastcgi",
    "mod_accesslog",
    "mod_dirlisting",
)

server.document-root = "/einar2"
index-file.names = ( "index.php" )
dir-listing.activate = "enable"

alias.url = (
    "/archetypes" => "/config/archetype.d"
)

fastcgi.server = ( ".php" =>
    ((
        "bin-path" => "/usr/bin/php-cgi",
        "socket" => "/tmp/php-fastcgi.socket",
        "max-procs" => 1,
        "bin-environment" => (
            "PHP_FCGI_CHILDREN" => "4",
            "PHP_FCGI_MAX_REQUESTS" => "10000"
        )
    ))
)
