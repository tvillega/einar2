services:

  einar2-registry:
    image: registry:3
    container_name: einar2-registry
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ../registry:/var/lib/registry/docker/registry
