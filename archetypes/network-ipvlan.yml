
  switch-{{ .NetworkDashed }}:
    name: switch-{{ .NetworkDashed }}
    driver: ipvlan
    driver_opts:
      parent: {{ .Subinterface }}
    ipam:
      config:
        - subnet: {{ .Network }}/{{ .Mask }}
          gateway: {{ .Gateway }}
